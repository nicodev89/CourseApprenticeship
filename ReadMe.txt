App is made of a database and an interface application.

The application allows control over the database (delete, search, modify etc.)

App is made by different classes as I was studying OOP with C# at the time.

This app was part of a college assignment with Finglas training centre